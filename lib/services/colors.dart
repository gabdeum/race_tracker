import 'package:flutter/material.dart';

const primaryColor = Color(0xFF5E35B1); // App bar and drawer
const primaryColorLight = Color(0xFF9162E4); // Bottom bar, Slider foreground
const primaryColorDark = Color(0xFF280680); // Buttons background, Slider background, Top bars
const primaryLightColor_50 = Color.fromRGBO(145, 98, 228, 0.5);
const primaryLightColor_20 = Color.fromRGBO(145, 98, 228, 0.2);

const primaryTextColor = Color(0xFFFFFFFF);
const secondaryTextColor = Color(0xFF000000);

const backgroundColor = Color(0xFFDCDCDC);